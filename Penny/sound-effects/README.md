# Penny Sound Effects

Drop `.mp3` files into this folder (or subfolders).

Penny's Soundboard panel auto-discovers every `.mp3` here and lets you play them without editing code.

Optional override:

- Set `PENNY_SFX_DIR` to use a different folder.
