{
  "mcpServers": {
    "veritas-kanban": {
      "command": "vk-mcp",
      "env": {
        "VK_API_URL": "${PENNY_VERITAS_API_URL}",
        "VK_API_KEY": "${PENNY_VERITAS_AGENT_KEY}",
        "VERITAS_ADMIN_KEY": "${PENNY_VERITAS_AGENT_KEY}"
      }
    }
  }
}
