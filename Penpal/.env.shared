# ── Shared config — committed to repo, loaded on all machines ─────────────
# Machine-specific overrides and SECRETS go in .env (gitignored)
#
# IMPORTANT: Do NOT put API keys or tokens in this file.
# Store them in your local .env instead.
#
# Required secrets (add to your local Penpal/.env):
#   SLACK_BOT_TOKEN=xoxb-...
#   SLACK_APP_TOKEN=xapp-...

SLACK_OWNER_USER_ID=U0FPCEC58
